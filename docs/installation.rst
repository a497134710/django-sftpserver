============
Installation
============

At the command line::

    $ easy_install django-sftpserver

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sftpserver
    $ pip install django-sftpserver
